extends layout/main
include mixins/videoBlock
block contents
    .home-videos
        each item in video
            +videoBlock({
                fileUrl:item.fileUrl,
                id:item.id,
                title:item.title,
                views:item.views
            })